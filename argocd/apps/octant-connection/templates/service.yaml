apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.ingressPort }}
      targetPort: {{ .Values.ingressPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}