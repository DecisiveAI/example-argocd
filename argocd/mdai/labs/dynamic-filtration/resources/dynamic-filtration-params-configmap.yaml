apiVersion: v1
kind: ConfigMap
metadata:
  name: dynamic-filtration-params
  namespace: mdai
data:
  HUB_NAME: mdaihub
  # Name of the generated variables ConfigMap that the collector should envFrom.
  # (If your operator uses a different naming convention, change this once per lab.)
  HUB_VARS_CM: mdaihub-variables
  GATEWAY_NAME: gateway-dynamic-filtration
